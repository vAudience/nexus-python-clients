# coding: utf-8

"""
    vAudience AI File Manager API

    API for the vAudience AI File Manager

    The version of the OpenAPI document: 0.22.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class CollectionResponse(BaseModel):
    """
    CollectionResponse
    """ # noqa: E501
    created_at: StrictStr
    description: Optional[StrictStr] = None
    file_count: Optional[StrictInt] = None
    id: StrictStr
    last_file_added_at: Optional[StrictStr] = Field(default=None, description="LastFileAddedAt is the creation time of the most recently added file in the collection. Omitted when the collection has no files.")
    name: StrictStr
    processors: Optional[Dict[str, Any]] = Field(default=None, description="Processors maps each enabled processor to its resource id (read-only), e.g. {\"deepr\": \"corpus-abc\"}.")
    status_breakdown: Optional[Dict[str, Dict[str, StrictInt]]] = Field(default=None, description="StatusBreakdown is keyed by processor name (v1: \"deepr\"). Each value is a per-status file count (e.g. {\"pending\": 5, \"completed\": 10}), derived from corpus file statuses. Omitted when no processor is enabled.")
    token_count: Optional[StrictInt] = Field(default=None, description="TokenCount is the collection's deepr ingested-token usage. Fail-soft: 0 when the collection has no provisioned corpus or the usage read is unavailable.")
    total_size: Optional[StrictInt] = None
    updated_at: StrictStr
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["created_at", "description", "file_count", "id", "last_file_added_at", "name", "processors", "status_breakdown", "token_count", "total_size", "updated_at"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CollectionResponse from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CollectionResponse from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "created_at": obj.get("created_at"),
            "description": obj.get("description"),
            "file_count": obj.get("file_count"),
            "id": obj.get("id"),
            "last_file_added_at": obj.get("last_file_added_at"),
            "name": obj.get("name"),
            "processors": obj.get("processors"),
            "status_breakdown": obj.get("status_breakdown"),
            "token_count": obj.get("token_count"),
            "total_size": obj.get("total_size"),
            "updated_at": obj.get("updated_at")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


