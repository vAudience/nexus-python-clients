# coding: utf-8

"""
    vAudience AI File Manager API

    API for the vAudience AI File Manager

    The version of the OpenAPI document: 0.22.1
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ProcessingStatus(BaseModel):
    """
    ProcessingStatus
    """ # noqa: E501
    error: Optional[StrictStr] = None
    error_code: Optional[StrictStr] = Field(default=None, description="ErrorCode is the structured terminal error code for a failed file, drawn from the same vocabulary as the `code` of an error response (e.g. \"corpus_token_quota_exceeded\"), so the same condition reads identically whether it surfaced as an HTTP error or as a status on a file. Two values occur only here -- \"corpus_processing_failed\" and \"upstream_processing_failed\" -- for failures the synchronous path can only report as a generic 500; see corpusStatusOnlyCodes in corpus_errors.go. Surfaced alongside Error so a token-quota failure is distinguishable from a generic one. Omitted when absent.")
    metadata: Optional[Dict[str, Any]] = None
    processed_at: Optional[StrictStr] = None
    status: Optional[StrictStr] = None
    token_count: Optional[StrictInt] = Field(default=None, description="TokenCount is the deepr-reported document token count for the file. Omitted while the count has not been fetched yet or the file failed, and present only once deepr reports one (typically on completion) — so a completed file may still omit it. Distinct from the collection-level token_count on CollectionResponse (the corpus-usage total, a different number).")
    additional_properties: Dict[str, Any] = {}
    __properties: ClassVar[List[str]] = ["error", "error_code", "metadata", "processed_at", "status", "token_count"]

    @field_validator('status')
    def status_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['pending', 'processing', 'completed', 'failed', 'disabled', 'unavailable']):
            raise ValueError("must be one of enum values ('pending', 'processing', 'completed', 'failed', 'disabled', 'unavailable')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ProcessingStatus from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * Fields in `self.additional_properties` are added to the output dict.
        """
        excluded_fields: Set[str] = set([
            "additional_properties",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # puts key-value pairs in additional_properties in the top level
        if self.additional_properties is not None:
            for _key, _value in self.additional_properties.items():
                _dict[_key] = _value

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ProcessingStatus from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "error": obj.get("error"),
            "error_code": obj.get("error_code"),
            "metadata": obj.get("metadata"),
            "processed_at": obj.get("processed_at"),
            "status": obj.get("status"),
            "token_count": obj.get("token_count")
        })
        # store additional fields in additional_properties
        for _key in obj.keys():
            if _key not in cls.__properties:
                _obj.additional_properties[_key] = obj.get(_key)

        return _obj


